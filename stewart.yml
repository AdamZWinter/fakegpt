version: "3"

services:
   stewartnode:
     container_name: "stewart"
     image: "node"
     restart: unless-stopped
    #  depends_on:
    #    - mariadb
     ports:
       - '8081:8080'
    #  links:
    #    - mariadb
     volumes:
       - /home/stewart/agile/app:/app:Z
#       - ../secrets:/var/www/secrets:z
#       - ./scripts:/var/www/scripts:z
      #  - ./logs:/var/www/logs:z
     command: node /app/server.js

# docker-compose -f stewart.yml up -d

# docker run -it --rm --name stewart -p 8081:8080 -v /home/winter/agile/app:/app:Z node /bin/bash